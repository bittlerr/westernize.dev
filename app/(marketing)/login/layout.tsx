export const metadata = {
  title: "Log in",
  description: "Log in to your Westernize account to optimize your CV for Western tech companies.",
  alternates: { canonical: "/login" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
