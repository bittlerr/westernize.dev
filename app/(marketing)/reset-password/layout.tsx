export const metadata = {
  title: "Reset Password",
  description: "Set a new password for your Westernize account.",
  alternates: { canonical: "/reset-password" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
