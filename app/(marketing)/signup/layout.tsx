export const metadata = {
  title: "Sign up",
  description: "Create a free Westernize account. Get 3 CV optimizations — no credit card required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
