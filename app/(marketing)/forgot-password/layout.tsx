export const metadata = {
  title: "Forgot Password",
  description: "Reset your Westernize account password.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
